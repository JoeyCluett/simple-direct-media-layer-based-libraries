#ifndef LINEDRAW_1
#define LINEDRAW_1
#include "LineObj.h"

class LineDraw{
public:
        void draw(Line, SDL_Surface* surface, int lineWidth = 1);
        int SortDelta(int i, int j);
        int ReturnLow(int i, int j);
        int ReturnHigh(int i, int j);
};
#endif // LINEDRAW_1
